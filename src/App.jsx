function App() {
  return (
    <div className="placeholder">
      <h1>Net Worth Tracker</h1>
      <p>M0 checkpoint: build + deploy pipeline is live.</p>
      <p className="note">
        Real UI (left panel, Dashboard, Holdings, etc.) starts at M2 — this page exists
        only to prove GitHub Actions → GitHub Pages works end to end.
      </p>
    </div>
  )
}

export default App
